from fastapi import APIRouter

router = APIRouter(prefix="/api/v1/rep-settings", tags=["rep-settings"])

# Rep settings management endpoints (e.g., ghost mode)
# This router can be extended with rep settings endpoints as needed


