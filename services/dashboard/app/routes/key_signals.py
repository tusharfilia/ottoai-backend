from fastapi import APIRouter

router = APIRouter(prefix="/api/v1/key-signals", tags=["key-signals"])

# Key signals management endpoints
# This router can be extended with key signals endpoints as needed


